/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', 'dummyimage.com', "picsum.photos", "placekitten.com", "lh3.googleusercontent.com" ], // Add both domains
  },
};

export default nextConfig;
